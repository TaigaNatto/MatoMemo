<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.t_robop.matomemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/matomemo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GroupEditActivity" />
        <!--<activity android:name=".StartListActivity" /> -->
        <activity android:name=".FolderCreateActivity" />
        <activity android:name=".MatoMemoListActivity" />
        <activity android:name=".RealmTestActivity" />
        <activity
            android:name=".WritingActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DBLogActivity" />
        <activity android:name=".MatomeActivity" />
        <activity android:name=".TagEditActivity" />
        <activity android:name=".KeyboardResizeTestActivity"
            android:windowSoftInputMode="adjustResize"></activity>
    </application>

</manifest>